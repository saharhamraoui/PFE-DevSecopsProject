name: monitoring-github

services:
  # Stackdriver exporter: pulls GCP Cloud Run metrics into Prometheus
  stackdriver-exporter:
    image: prometheuscommunity/stackdriver-exporter:latest
    environment:
      - GOOGLE_APPLICATION_CREDENTIALS=/credentials/gcp-key.json
      - GOOGLE_PROJECT_ID=sahar-pfe
    volumes:
      - /home/sahar/gcp-key-github.json:/credentials/gcp-key.json:ro
    ports:
      - "9255:9255"
    command:
      - "--google.project-id=sahar-pfe"
      - "--monitoring.metrics-type-prefixes=run.googleapis.com"
      - "--monitoring.drop-delegated-projects"
    restart: unless-stopped

  prometheus:
    image: prom/prometheus:latest
    user: root
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus_data:/prometheus
    ports:
      - "9090:9090"
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.retention.time=7d"
    restart: unless-stopped
    depends_on:
      - stackdriver-exporter

  grafana:
    image: grafana/grafana:latest
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - GF_USERS_ALLOW_SIGN_UP=false
    volumes:
      - grafana_data:/var/lib/grafana
      - ./grafana/provisioning:/etc/grafana/provisioning:ro
    ports:
      - "3000:3000"
    restart: unless-stopped
    depends_on:
      - prometheus

  sonarqube:
    image: sonarqube:community
    container_name: sonarqube-github
    mem_limit: 2g
    ports:
      - "9000:9000"
    environment:
      SONAR_ES_BOOTSTRAP_CHECKS_DISABLE: "true"
    volumes:
      - sonarqube_data:/opt/sonarqube/data
      - sonarqube_logs:/opt/sonarqube/logs
    restart: unless-stopped

volumes:
  prometheus_data:
  grafana_data:
  sonarqube_data:
  sonarqube_logs:
